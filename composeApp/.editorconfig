# ktlint configuration for Compose
root = false

[*.kt]
# Allow PascalCase function names for Composable functions (Compose convention)
ktlint_function_naming_ignore_when_annotated_with = Composable

# Standard indentation
indent_size = 4

# Allow single-line if without braces for simple cases
ktlint_indent_size = 4

